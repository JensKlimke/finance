{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "start": "npx lerna run start",
    "test": "npx lerna run test"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^6.5.1",
    "nodemon": "^2.0.21",
    "serve": "^14.2.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.5"
  }
}
